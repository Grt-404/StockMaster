<!DOCTYPE html>
<html data-wf-domain="www.stockmaster.app" data-wf-page="67a323a1c68594827fa0ccb0" data-wf-site="67a323a1c68594827fa0cca9" lang="en">
<head>
    <meta charset="utf-8"/>
    <title>StockMaster | Security</title>
    <meta content="width=device-width, initial-scale=1" name="viewport"/>
    
    <link href="https://cdn.prod.website-files.com/67a323a1c68594827fa0cca9/css/soscale-39349c-c66c87b5666753b1b89d5bc1.webflow.shared.1aa2f82cd.css" rel="stylesheet" type="text/css"/>
    <link href="https://fonts.googleapis.com" rel="preconnect"/>
    <link href="https://fonts.gstatic.com" rel="preconnect" crossorigin="anonymous"/>
    <script src="https://ajax.googleapis.com/ajax/libs/webfont/1.6.26/webfont.js" type="text/javascript"></script>
    <script type="text/javascript">WebFont.load({  google: {    families: ["Montserrat:100,100italic,200,200italic,300,300italic,400,400italic,500,500italic,600,600italic,700,700italic,800,800italic,900,900italic","Oswald:200,300,400,500,600,700"]  }});</script>
    <script type="text/javascript">!function(o,c){var n=c.documentElement,t=" w-mod-";n.className+=t+"js",("ontouchstart"in o||o.DocumentTouch)&&(n.className+=t+"touch")}(window,document);</script>
    <link href="https://cdn.prod.website-files.com/67a323a1c68594827fa0cca9/67a392515bb29399f8c12d07_fav.png" rel="shortcut icon" type="image/x-icon"/>
    <link href="https://cdn.prod.website-files.com/67a323a1c68594827fa0cca9/67a323a1c68594827fa0ce4b_fav_256.png" rel="apple-touch-icon"/>
    
    <link href="https://cdn.jsdelivr.net/npm/splitting@1.0.6/dist/splitting.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/gsap@3.12.5/dist/gsap.min.js" defer ></script>
    <script src="https://cdn.jsdelivr.net/npm/gsap@3.12.5/dist/ScrollTrigger.min.js" defer></script>
    <script src="https://cdn.jsdelivr.net/gh/timothydesign/script/split-type.js" defer></script>

    <style>
        /* --- Utilities from Dashboard --- */
        .w-webflow-badge { display: none !important; } 
        html.lenis { height: auto; }
        .lenis.lenis-smooth { scroll-behavior: auto; }
        .heading-scroll .char { transform: translateZ(0) !important; }
        
        /* Letter Wrap Animation */
        .letter-wrap { display: inline-block; position: relative; } 
        .letter-wrap * { pointer-events: none; } 
        .letter-wrap__word { display: inline-flex; position: relative; overflow: hidden; } 
        .letter-wrap__char { display: inline-block; position: relative; text-transform: uppercase; } 
        .letter-wrap__char-inner { display: inline-block; } 
        .letter-wrap__char-inner:after { content: attr(data-letter); position: absolute; top: 100%; left: 0; }

        /* --- DASHBOARD LAYOUT --- */
        .dashboard-section { 
            padding-top: 140px; /* Slightly reduced from 160px since ticker is gone */
            padding-bottom: 100px; 
            min-height: 100vh; 
            background-color: #0e0e0e; 
            color: #edf2f4; 
            position: relative;
            z-index: 2;
            background-image: radial-gradient(circle at 50% 50%, #1a1a1a 0%, #0e0e0e 100%);
        }

        /* --- HEADER --- */
        .dashboard-nav {
            position: fixed;
            top: 0; left: 0; right: 0;
            height: 90px;
            padding: 0 40px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: rgba(14, 14, 14, 0.7);
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            border-bottom: 1px solid rgba(255, 255, 255, 0.08);
            z-index: 1000;
            transition: all 0.3s ease;
        }

        .nav-left { display: flex; align-items: center; gap: 30px; }
        .nav-logo-text {
            font-family: 'Oswald', sans-serif;
            font-size: 24px;
            color: #fff;
            text-transform: uppercase;
            letter-spacing: 1px;
            font-weight: 500;
            text-decoration: none;
        }

        .nav-center {
            display: flex;
            gap: 10px;
            background: rgba(255, 255, 255, 0.03);
            padding: 5px;
            border-radius: 50px;
            border: 1px solid rgba(255, 255, 255, 0.05);
        }

        .nav-pill {
            padding: 8px 20px;
            border-radius: 20px;
            color: #888;
            text-decoration: none;
            font-family: 'Montserrat', sans-serif;
            font-size: 12px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }
        .nav-pill:hover { color: #fff; background: rgba(255, 255, 255, 0.1); }
        .nav-pill.active { color: #000; background: #fff; font-weight: 600; }

        .nav-right { display: flex; align-items: center; gap: 20px; }
        .user-status {
            display: flex;
            align-items: center;
            gap: 8px;
            font-family: 'Oswald', sans-serif;
            font-size: 14px;
            color: #fff;
        }
        .status-dot {
            width: 8px;
            height: 8px;
            background: #4dff88;
            border-radius: 50%;
            box-shadow: 0 0 8px #4dff88;
            animation: pulse 2s infinite;
        }
        @keyframes pulse { 0% { opacity: 0.5; } 50% { opacity: 1; } 100% { opacity: 0.5; } }

        .logout-btn {
            display: flex;
            align-items: center;
            gap: 8px;
            padding: 8px 16px;
            border: 1px solid rgba(255,255,255,0.2);
            border-radius: 4px;
            color: #fff;
            text-decoration: none;
            font-family: 'Oswald', sans-serif;
            text-transform: uppercase;
            font-size: 12px;
            transition: all 0.3s;
        }
        .logout-btn:hover { background: #fff; color: #000; border-color: #fff; }

        /* Layout & Sidebar */
        .dashboard-container {
            max-width: 1400px;
            width: 95vw;
            margin: 40px auto 0;
            display: grid;
            grid-template-columns: 240px 1fr;
            gap: 40px;
        }

        .sidebar-menu { border-right: 1px solid rgba(255,255,255,0.1); padding-right: 20px; }
        .sidebar-header {
            font-family: 'Montserrat', sans-serif;
            font-size: 10px;
            text-transform: uppercase;
            letter-spacing: 2px;
            color: #666;
            margin-bottom: 15px;
            margin-top: 30px;
        }
        .sidebar-link {
            display: block;
            font-family: 'Oswald', sans-serif;
            font-size: 18px;
            text-transform: uppercase;
            color: #888;
            text-decoration: none;
            padding: 10px 0;
            transition: all 0.3s ease;
            position: relative;
        }
        .sidebar-link:hover, .sidebar-link.active { color: #fff; padding-left: 10px; }
        .sidebar-link.active::before {
            content: '';
            position: absolute; left: 0; top: 50%; transform: translateY(-50%);
            width: 4px; height: 4px; background: #4dff88; border-radius: 50%;
        }

        /* --- SECURITY SPECIFIC STYLES (Matching Dashboard Theme) --- */
        .security-card {
            background: #121212;
            border: 1px solid rgba(255,255,255,0.1);
            padding: 40px; 
            position: relative;
            overflow: hidden;
            margin-bottom: 30px;
        }
        .security-card::before {
            content: '';
            position: absolute; top: 0; left: 0; width: 100%; height: 100%;
            background: radial-gradient(circle at top right, rgba(255,255,255,0.05), transparent 60%);
            pointer-events: none;
        }

        .form-group { margin-bottom: 25px; }
        .form-label { 
            display: block; 
            color: #888; 
            margin-bottom: 10px; 
            font-family: 'Oswald', sans-serif; 
            text-transform: uppercase; 
            font-size: 12px; 
            letter-spacing: 1px; 
        }
        .form-input { 
            width: 100%; 
            padding: 15px; 
            background: #0e0e0e; 
            border: 1px solid rgba(255,255,255,0.1); 
            color: #fff; 
            font-family: 'Montserrat', sans-serif; 
            font-size: 14px; 
            transition: 0.3s; 
        }
        .form-input:focus { 
            border-color: #4dff88; 
            outline: none; 
            background: rgba(255,255,255,0.05);
        }
        
        .save-btn { 
            background: transparent; 
            color: #fff; 
            padding: 12px 30px; 
            border: 1px solid #fff; 
            font-family: 'Oswald', sans-serif; 
            text-transform: uppercase; 
            font-weight: 400; 
            cursor: pointer; 
            letter-spacing: 1px; 
            transition: 0.3s; 
            margin-top: 10px;
            display: inline-block;
        }
        .save-btn:hover { 
            background: #fff; 
            color: #000; 
            box-shadow: 0 0 15px rgba(255,255,255,0.3); 
        }

        /* Toggle Switch */
        .toggle-wrapper { 
            display: flex; 
            align-items: center; 
            justify-content: space-between; 
            padding: 20px 0; 
            border-bottom: 1px solid rgba(255,255,255,0.05); 
        }
        .toggle-switch { 
            position: relative; 
            width: 50px; 
            height: 26px; 
            background: #333; 
            border-radius: 20px; 
            cursor: pointer; 
            transition: 0.3s; 
        }
        .toggle-switch::after { 
            content: ''; 
            position: absolute; 
            top: 3px; left: 3px; 
            width: 20px; height: 20px; 
            background: white; 
            border-radius: 50%; 
            transition: 0.3s; 
        }
        .toggle-active { background: #4dff88; }
        .toggle-active::after { transform: translateX(24px); }

        /* Alerts */
        .alert-box {
            padding: 15px;
            margin-bottom: 20px;
            font-family: 'Oswald', sans-serif;
            text-transform: uppercase;
            font-size: 12px;
            letter-spacing: 1px;
            border: 1px solid;
        }
        .alert-success { background: rgba(77, 255, 136, 0.1); color: #4dff88; border-color: #4dff88; }
        .alert-error { background: rgba(255, 77, 77, 0.1); color: #ff4d4d; border-color: #ff4d4d; }

        /* Footer */
        .footer-divider { height: 1px; background-color: rgba(255, 255, 255, 0.1); margin-top: 100px; margin-bottom: 40px; }
        .div-block-75 { display: flex; justify-content: space-between; align-items: center; color: #666; font-family: 'Montserrat', sans-serif; font-size: 12px; text-transform: uppercase; }
        .text-block-7-copy { color: #666; text-decoration: none; transition: color 0.3s; }
        .text-block-7-copy:hover { color: #fff; }

        @media (max-width: 991px) {
            .dashboard-nav { padding: 0 20px; height: 70px; }
            .nav-center { display: none; } 
            .dashboard-container { grid-template-columns: 1fr; gap: 40px; }
        }
    </style>
</head>

<body class="body">

    <div data-w-id="edddb9d9-c6c4-45f3-fed7-241709972d7b" class="preloader-parent">
        <div class="logoname-wrapper">
            <div class="black-shape-top"></div>
            <div class="black-shape-logo-bot">
                <img src="https://cdn.prod.website-files.com/67a323a1c68594827fa0cca9/67a323a1c68594827fa0cd29_SOSCALE-logo2%201.svg" loading="lazy" alt="" class="logo-preloader-color"/>
            </div>
        </div>
    </div>

    <nav class="dashboard-nav">
        <div class="nav-left">
            <a href="/" class="w-inline-block" style="text-decoration: none;">
                <div class="nav-logo-text">Stock Master</div>
            </a>
        </div>

        <div class="nav-center">
            <a href="/home" class="nav-pill">
                <span class="letter-wrap">Dashboard</span>
            </a>
            <a href="/products" class="nav-pill">
                <span class="letter-wrap">Products</span>
            </a>
            <a href="/operations" class="nav-pill">
                <span class="letter-wrap">Operations</span>
            </a>
            <a href="/warehouses/map" class="nav-pill">
                <span class="letter-wrap">Map</span>
            </a>
        </div>

        <div class="nav-right">
            <div class="user-status">
                <div class="status-dot"></div>
                <span>Live</span>
            </div>
            <a href="/users/logout" class="logout-btn">
                Logout
                <img src="https://cdn.prod.website-files.com/67a323a1c68594827fa0cca9/67a323a1c68594827fa0ccb4_Vector.svg" width="12" alt="" style="filter: invert(1);">
            </a>
        </div>
    </nav>

    <section class="dashboard-section">
        <div class="dashboard-container">
            
            <aside class="sidebar-menu">
                <div class="sidebar-header">Back</div>
                <a href="/home" class="sidebar-link">← Dashboard</a>
                
                <div class="sidebar-header">Account Settings</div>
                <div style="color: #fff; font-family:'Oswald'; font-size: 20px; margin-bottom: 5px;">
                     My Account
                </div>
                
                <a href="/profile" class="sidebar-link">Personal Details</a>
                <a href="/profile/security" class="sidebar-link active">Security</a>
                <a href="/profile/notifications" class="sidebar-link">Notifications</a>
                
                <div class="sidebar-header">System</div>
                <a href="/users/logout" class="sidebar-link" style="color: #ff4d4d;">Logout</a>
            </aside>

            <div class="main-content">
                
                <div style="margin-bottom: 30px; border-bottom: 1px solid rgba(255,255,255,0.1); padding-bottom: 20px;">
                    <h1 class="heading-scroll _9 size-28" style="font-size: 40px; margin: 0;">Login & Security</h1>
                    <div style="color: #888; font-family: 'Montserrat'; font-size: 14px; margin-top: 5px;">
                        Manage your password and 2FA settings.
                    </div>
                </div>

                <% if (typeof success !== 'undefined' && success && success.length > 0) { %>
                    <div class="alert-box alert-success"><%= success %></div>
                <% } %>
                <% if (typeof error !== 'undefined' && error && error.length > 0) { %>
                    <div class="alert-box alert-error"><%= error %></div>
                <% } %>

                <div style="display: grid; grid-template-columns: 1fr; gap: 30px; max-width: 800px;">
                    
                    <div class="security-card">
                        <h3 style="font-family: 'Oswald'; font-size: 20px; margin-bottom: 20px; color: white; border-bottom: 1px solid rgba(255,255,255,0.1); padding-bottom: 10px;">
                            Change Password
                        </h3>
                        <form action="/profile/security/update-password" method="POST">
                            <div class="form-group">
                                <label class="form-label">Current Password</label>
                                <input type="password" name="currentPassword" class="form-input" required>
                            </div>
                            <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px;">
                                <div class="form-group">
                                    <label class="form-label">New Password</label>
                                    <input type="password" name="newPassword" class="form-input" required>
                                </div>
                                <div class="form-group">
                                    <label class="form-label">Confirm New Password</label>
                                    <input type="password" name="confirmPassword" class="form-input" required>
                                </div>
                            </div>
                            <button type="submit" class="save-btn">Update Password</button>
                        </form>
                    </div>

                    <div class="security-card">
                        <h3 style="font-family: 'Oswald'; font-size: 20px; margin-bottom: 20px; color: white; border-bottom: 1px solid rgba(255,255,255,0.1); padding-bottom: 10px;">
                            Two-Factor Authentication
                        </h3>
                        <div class="toggle-wrapper">
                            <div>
                                <div style="font-family: 'Montserrat'; font-weight: 600; font-size: 14px; color: #fff;">Email Authentication</div>
                                <div style="font-family: 'Montserrat'; font-size: 12px; color: #666; margin-top: 5px;">Receive a code via email when logging in from a new device.</div>
                            </div>
                            <div class="toggle-switch toggle-active" onclick="this.classList.toggle('toggle-active')"></div>
                        </div>
                        <div class="toggle-wrapper" style="border-bottom: none;">
                            <div>
                                <div style="font-family: 'Montserrat'; font-weight: 600; font-size: 14px; color: #fff;">SMS Authentication</div>
                                <div style="font-family: 'Montserrat'; font-size: 12px; color: #666; margin-top: 5px;">Receive a code via SMS (Premium Feature).</div>
                            </div>
                            <div class="toggle-switch" onclick="this.classList.toggle('toggle-active')"></div>
                        </div>
                    </div>

                </div>

            </div>
        </div>
    </section>

    <section class="footer">
        <div class="dashboard-container">
            <div class="footer-divider"></div>
            <div class="div-block-75">
                <div class="text-block-7">StockMaster IMS v1.0</div>
                <a href="#" class="text-block-7-copy">© 2025 StockMaster</a>
            </div>
        </div>
    </section>

    <script src="https://d3e54v103j8qbb.cloudfront.net/js/jquery-3.5.1.min.dc5e7f18c8.js?site=67a323a1c68594827fa0cca9" type="text/javascript" integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0=" crossorigin="anonymous"></script>
    <script src="https://cdn.prod.website-files.com/67a323a1c68594827fa0cca9/js/webflow.schunk.9cd7b152627b8cd1.js" type="text/javascript"></script>
    <script src="https://cdn.jsdelivr.net/gh/studio-freight/lenis@1.0.23/bundled/lenis.min.js"></script>

    <script>
        // Letter Wrap (Hover Effect)
        const letterWrapClass = 'letter-wrap';
        const letterWrapElements = document.getElementsByClassName(letterWrapClass);
        [...letterWrapElements].forEach(el => {
            letterWrap(el, letterWrapClass);
            letterAnimation(el, letterWrapClass);
        });
        function letterWrap(el, cls) {
            const words = el.textContent.split(' ');
            const letters = [];
            cls = cls || 'letter-wrap'
            words.forEach(word => {
                let html = '';
                for (var letter in word) {
                    html += <span class="${cls}__char"><span class="${cls}__char-inner" data-letter="${word[letter]}">${word[letter]}</span></span>;
                };
                let wrappedWords = <span class="${cls}__word">${html}</span>;
                letters.push(wrappedWords);
            });
            return el.innerHTML = letters.join(' ');
        }

        function letterAnimation(el, cls) {
            const tl = new TimelineMax({ paused: true });
            const characters = el.querySelectorAll(.${cls}__char-inner);
            el.animation = tl.staggerTo(characters, 0.3, { y: '-100%', delay: 0.05, ease: Power2.easeOut }, 0.025);
            el.addEventListener('mouseenter', (event) => event.currentTarget.animation.play());
            el.addEventListener('mouseout', (event) => el.animation.pause(0));
        }

        // Smooth Scroll
        if (window.innerWidth > 780) {
            const lenis = new Lenis({ lerp: 0.1, smooth: true });
            function raf(time) { lenis.raf(time); requestAnimationFrame(raf); }
            requestAnimationFrame(raf);
        }
    </script>
</body>
</html>