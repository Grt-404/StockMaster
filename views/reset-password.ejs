<!DOCTYPE html>
<html lang="en" class="bg-black">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reset Password | StockMaster</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@100;300;400;500;600&family=Oswald:wght@200;400;500&display=swap" rel="stylesheet">
    <script type="module" src="https://unpkg.com/@splinetool/viewer@1.9.59/build/spline-viewer.js"></script>
    
    <style>
        body { font-family: 'Montserrat', sans-serif; overflow-x: hidden; }
        h1, h2, h3 { font-family: 'Oswald', sans-serif; }
        /* Reuse previous premium styles */
        input:-webkit-autofill, input:-webkit-autofill:hover, input:-webkit-autofill:focus {
            -webkit-box-shadow: 0 0 0 30px #09090b inset !important;
            -webkit-text-fill-color: white !important;
            transition: background-color 5000s ease-in-out 0s;
        }
        .premium-border { position: relative; }
        .premium-border::after {
            content: ''; position: absolute; inset: -1px; border-radius: inherit;
            background: linear-gradient(to bottom right, rgba(255,255,255,0.2), rgba(255,255,255,0.05), rgba(255,255,255,0));
            z-index: -1; pointer-events: none;
        }
    </style>
</head>
<body class="bg-black text-white h-screen w-full relative flex items-center justify-center">

    <div class="absolute inset-0 z-0 opacity-50">
        <spline-viewer url="https://prod.spline.design/os6NRsumsgqdhK5E/scene.splinecode"></spline-viewer>
    </div>

    <div class="relative z-20 w-full max-w-md p-6 md:p-8">
        <div class="text-center mb-10">
            <h2 class="text-4xl font-bold uppercase tracking-tighter text-transparent bg-clip-text bg-gradient-to-b from-white to-zinc-500">New Password</h2>
            <p class="text-zinc-400 text-xs mt-2 tracking-[0.2em] uppercase">Secure your account</p>
        </div>

        <div class="premium-border bg-zinc-900/40 backdrop-blur-2xl p-8 rounded-none shadow-2xl">
            
            <form action="/reset-password/<%= id %>/<%= token %>" method="POST" class="flex flex-col gap-6">
                <div class="group relative">
                    <input class="w-full bg-transparent border-b border-zinc-700 py-3 text-white placeholder-transparent focus:outline-none focus:border-white transition-colors peer" 
                           type="password" name="password" id="password" required>
                    <label for="password" class="absolute left-0 -top-3.5 text-zinc-500 text-xs transition-all peer-placeholder-shown:text-base peer-placeholder-shown:text-zinc-500 peer-placeholder-shown:top-3 peer-focus:-top-3.5 peer-focus:text-white peer-focus:text-xs">New Password</label>
                </div>

                <button type="submit" class="w-full mt-4 bg-white text-black py-3 font-bold uppercase tracking-wider text-sm hover:bg-zinc-200 transition-colors">
                    Update Password
                </button>
            </form>

        </div>
    </div>
</body>
</html>