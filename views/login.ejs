<!DOCTYPE html>
<html data-wf-domain="www.stockmaster.app" lang="en">
<head>
    <meta charset="utf-8"/>
    <title>StockMaster | Login</title>
    <meta content="width=device-width, initial-scale=1" name="viewport"/>
    
    <link href="https://fonts.googleapis.com" rel="preconnect"/>
    <link href="https://fonts.gstatic.com" rel="preconnect" crossorigin="anonymous"/>
    <script src="https://ajax.googleapis.com/ajax/libs/webfont/1.6.26/webfont.js" type="text/javascript"></script>
    <script type="text/javascript">WebFont.load({  google: {    families: ["Montserrat:100,100italic,200,200italic,300,300italic,400,400italic,500,500italic,600,600italic,700,700italic,800,800italic,900,900italic","Oswald:200,300,400,500,600,700"]  }});</script>
    
    <script src="https://cdn.jsdelivr.net/npm/gsap@3.12.5/dist/gsap.min.js" defer></script>

    <style>
        /* --- StockMaster Theme Core --- */
        body { 
            background-color: #0e0e0e; 
            color: #edf2f4; 
            font-family: 'Montserrat', sans-serif; 
            margin: 0;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background-image: radial-gradient(circle at 50% 50%, #1a1a1a 0%, #0e0e0e 100%);
            overflow: hidden;
        }

        /* Typography */
        h1, h2 { font-family: 'Oswald', sans-serif; text-transform: uppercase; }
        
        /* Login Card Container - Matches Dashboard Cards */
        .login-card {
            background: #121212;
            border: 1px solid rgba(255,255,255,0.1);
            padding: 50px;
            width: 100%;
            max-width: 450px;
            position: relative;
            box-shadow: 0 20px 50px rgba(0,0,0,0.5);
            z-index: 2;
            opacity: 0; /* for animation */
            transform: translateY(20px); /* for animation */
        }

        .login-header {
            text-align: center;
            margin-bottom: 40px;
        }

        .brand-title {
            font-size: 48px;
            color: #fff;
            letter-spacing: 2px;
            margin: 0;
            line-height: 1;
        }

        .brand-subtitle {
            font-size: 12px;
            color: #666;
            letter-spacing: 1px;
            margin-top: 10px;
        }

        /* Form Elements matching Operations Page */
        .form-group { margin-bottom: 25px; position: relative; }
        
        .form-label { 
            display: block; 
            color: #888; 
            margin-bottom: 8px; 
            font-family: 'Oswald', sans-serif; 
            text-transform: uppercase; 
            font-size: 12px; 
            letter-spacing: 1px;
        }

        .form-input { 
            width: 100%; 
            box-sizing: border-box;
            padding: 15px; 
            background: #0e0e0e; 
            border: 1px solid rgba(255,255,255,0.1); 
            color: white; 
            font-family: 'Montserrat', sans-serif; 
            font-size: 14px;
            transition: 0.3s;
            border-radius: 2px;
        }

        .form-input:focus { 
            border-color: #4dff88; 
            outline: none; 
            box-shadow: 0 0 15px rgba(77, 255, 136, 0.1);
        }

        /* Animated Button */
        .login-btn {
            display: block;
            width: 100%;
            padding: 15px;
            background: #fff;
            color: #000;
            text-transform: uppercase;
            font-family: 'Oswald', sans-serif;
            font-size: 16px;
            border: none;
            cursor: pointer;
            transition: all 0.3s cubic-bezier(0.165, 0.84, 0.44, 1);
            margin-top: 10px;
            letter-spacing: 1px;
        }

        .login-btn:hover {
            background: #4dff88;
            box-shadow: 0 0 20px rgba(77, 255, 136, 0.4);
        }

        /* Links */
        .auth-links {
            margin-top: 30px;
            display: flex;
            justify-content: space-between;
            font-size: 11px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .auth-link {
            color: #666;
            text-decoration: none;
            transition: 0.3s;
        }

        .auth-link:hover { color: #4dff88; }

        /* Flash Messages */
        .flash-message {
            position: absolute;
            top: -60px;
            left: 0;
            width: 100%;
            background: rgba(255, 77, 77, 0.1);
            border: 1px solid #ff4d4d;
            color: #ff4d4d;
            padding: 12px;
            font-family: 'Oswald';
            text-transform: uppercase;
            font-size: 12px;
            text-align: center;
            box-sizing: border-box;
        }

        /* Decorative Background Elements */
        .bg-shape {
            position: absolute;
            filter: blur(100px);
            z-index: 1;
            opacity: 0.4;
        }
        .shape-1 {
            top: -10%;
            left: -10%;
            width: 500px;
            height: 500px;
            background: #4dff88;
        }
        .shape-2 {
            bottom: -10%;
            right: -10%;
            width: 400px;
            height: 400px;
            background: #333;
        }
    </style>
</head>
<body>

    <div class="bg-shape shape-1"></div>
    <div class="bg-shape shape-2"></div>

    <div class="login-card">
        
        <% if(typeof error !== 'undefined' && error.length > 0){ %>
            <div class="flash-message" id="flash-msg">
                <%= error %>
            </div>
        <% } %>

        <div class="login-header">
            <h1 class="brand-title">Stock Master</h1>
            <div class="brand-subtitle">Inventory Management System Access</div>
        </div>

        <form autocomplete="off" action="/users/login" method="post">
            <div class="form-group">
                <label for="LoginID" class="form-label">Login ID</label>
                <input class="form-input" type="text" name="LoginID" id="LoginID" required>
            </div>

            <div class="form-group">
                <label for="password" class="form-label">Password</label>
                <input class="form-input" type="password" name="password" id="password" required>
            </div>

            <button type="submit" class="login-btn">
                Initialize Session
            </button>
        </form>

        <div class="auth-links">
            <a href="/register_user" class="auth-link">Create Account</a>
            <a href="/forgot-password" class="auth-link">Forgot Password?</a>
        </div>
    </div>

    <script>
        // Entrance Animation matching the smooth feel of the dashboard
        document.addEventListener("DOMContentLoaded", (event) => {
            gsap.to(".login-card", {
                duration: 0.8, 
                y: 0, 
                opacity: 1, 
                ease: "power3.out",
                delay: 0.2
            });

            // Flash message auto-dismiss
            const flash = document.getElementById('flash-msg');
            if (flash) {
                gsap.from(flash, { y: -10, opacity: 0, duration: 0.5 });
                setTimeout(() => {
                    gsap.to(flash, { opacity: 0, y: -10, duration: 0.5, onComplete: () => flash.remove() });
                }, 4000);
            }
        });
    </script>
</body>
</html>